name: Trigger auto deployment for smart-bank

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/smart-bank-AutoDeployTrigger-7825ecf5-fbc8-4ec6-a471-bd29d9ff5d0d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SMARTBANK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SMARTBANK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SMARTBANK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}Banking_Ai
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: mysmartbank.azurecr.io
          registryUsername: ${{ secrets.SMARTBANK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SMARTBANK_REGISTRY_PASSWORD }}
          containerAppName: smart-bank
          resourceGroup: banking-ai
          imageToBuild: mysmartbank.azurecr.io/smart-bank:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


